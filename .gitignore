node_modules/
dist/
.env
.env.local
.DS_Store
*.log
__pycache__/
*.pyc

# Local Claude Code config + worktrees (machine-specific, not for a public repo)
.claude/settings.local.json
.claude/worktrees/

# Supabase CLI local link state (contains project ref)
supabase/.temp/

# Exported conversation transcripts (contain emails, keys, full session detail).
# `/export` names files `YYYY-MM-DD-HHMMSS-<conversation-title>.txt`, so match the date prefix —
# an earlier attempt matched `*-resume.txt` and missed every export whose title wasn't "resume".
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
*-resume.txt
